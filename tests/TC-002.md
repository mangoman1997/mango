# TC-002: 專案/任務建立與指派 (ShopperProxy)
- **前置條件**: 買家角色已登入，具備建立代購任務之權限。
- **步驟**: 
  1. POST /api/tasks 發送代購需求(包含商品網址、預期價格、地點)。
  2. 系統寫入 DB 且狀態設為 'OPEN'。
  3. 旅人角色 GET /api/tasks 獲取列表並申請接單 (POST /api/tasks/{id}/apply)。
- **預期結果**: 任務成功建立，旅人申請後狀態變更為 'PENDING_MATCH'。
